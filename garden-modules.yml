---
kind: Module
description: mysql chart for use with backend
type: helm
name: mysql
chart: stable/mysql
version: "1.6.2"
values:
  mysqlUser: arbor_user
  mysqlPassword: arbor_password
  mysqlDatabase: arbor_db
---
# kind: Module
# type: exec
# name: docker-registry
# description: Add secrets for private docker registry
# local: true
# exclude:
#   - "."
# build:
#   command:
#     - kubectl -n default create secret docker-registry dockerhub-registry
#     - --docker-server="https://index.docker.io/v1/"
#     - --docker-username=${local.env.DOCKERHUB_USERNAME}
#     - --docker-password=${local.env.DOCKERHUB_TOKEN}
#     - --dry-run -o yaml | kubectl apply -f -